name: Update README

on:
  schedule:
    - cron: "0 * * * *"   # runs every hour (you can change to daily: "0 0 * * *")
  workflow_dispatch:      # allow manual trigger

jobs:
  update:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repo
        uses: actions/checkout@v3

      - name: Update README
        run: |
          echo "## Hello, I'm PaleExe 👋" > README.md
          echo "" >> README.md
          echo "### Latest update: $(date)" >> README.md
          echo "" >> README.md
          echo "- 🔧 Working on cool projects" >> README.md
          echo "- 🦀 Rust, Python, Angular Motivated" >> README.md

      - name: Commit & Push changes
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add README.md
          git commit -m "chore: update README [skip ci]" || echo "No changes"
          git push
